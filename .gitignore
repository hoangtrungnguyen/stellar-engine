# macOS
.DS_Store

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/

# task-generator runtime artifacts (live in target repos, not here)
runs/

# generator agent runtime artifacts — operator-local, never committed
drafts/

# Local Plane sync state
.plane-pages.json

# Claude local settings
.claude/settings.local.json

# Local environment / API keys — NEVER commit
.env
.env.local
.env.*.local

# PyInstaller build artifacts
.build-venv/
build/
dist/

# Vendored grava assets — populated at build time by
# scripts/vendor-grava-assets.sh; never committed.
vendored/

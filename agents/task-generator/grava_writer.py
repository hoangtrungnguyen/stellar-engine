"""Grava writer (Phase 3 deliverable). Phase 1 stub raises NotImplementedError."""


def execute(plan, target_repo, state):
    raise NotImplementedError(
        "grava_writer.execute is a Phase 3 deliverable. "
        "Run with --dry-run or --no-grava for Phase 1 behaviour."
    )

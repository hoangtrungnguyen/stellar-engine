# se-sandbox

## System type

**Type:** sandbox — end-to-end test environment for stellar-engine. Not a real system. Safe to reset, re-init, or wipe at any time.

---

## Repo

Path: `/home/htnguyen/Space/se-sandbox`

---

## Purpose

Used to validate `se` CLI commands end-to-end without touching real Plane projects or production repos. Run any `se` command against this folder to test behavior safely.
